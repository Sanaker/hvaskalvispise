<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ingen package attributt her lenger -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hvaskalvispise"
        tools:targetApi="31">
    <activity
        android:name=".ui.DishListActivity"
        android:exported="false"
        android:label="@string/title_activity_dish_list"
        android:theme="@style/Theme.Hvaskalvispise.NoActionBar" /> <!-- Dette er tema for DishListActivity -->
    <activity
        android:name=".ui.MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>
    </manifest>
